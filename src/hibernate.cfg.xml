<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/unimore_3</property>
        <property name="connection.username">root</property>
        <property name="connection.password"></property>

        <!-- JDBC connection pool (use the built-in) -->
        <!--property name="connection.pool_size">1</property-->


        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="current_session_context_class">thread</property>

        <property name="hibernate.connection.zeroDateTimeBehavior">convertToNull</property>
        <property name="hibernate.connection.zeroDateBehavior">convertToNull</property>
        <property name="hibernate.c3p0.min_size">3</property>
        <property name="hibernate.c3p0.max_size">100</property>
        <property name="hibernate.c3p0.timeout">900</property>
        <property name="hibernate.c3p0.max_statements">100</property>

        <!-- Disable the second-level cache  -->


        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">UPDATE</property>


        <mapping resource="com/auleSVGWT/server/domain/Person.hbm.xml"/>
        <mapping resource="com/auleSVGWT/server/domain/Building.hbm.xml"/>
        <mapping resource="com/auleSVGWT/server/domain/Role.hbm.xml"/>
        <mapping resource="com/auleSVGWT/server/domain/Room.hbm.xml"/>
        <mapping resource="com/auleSVGWT/server/domain/Occupy.hbm.xml"/>


    </session-factory>

</hibernate-configuration>